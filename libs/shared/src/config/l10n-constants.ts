export namespace l10n {
  export const event_day_exit = 'Event overlaps with an existing event';
  export const cant_be_equal = 'A unique constraint violation occurred';
  export const start_before_must = 'Start date must be before end date';
  
  
}
